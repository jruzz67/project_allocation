fastapi==0.115.0          # or latest, but pin for stability
uvicorn[standard]==0.32.0
sqlmodel==0.0.22          # latest stable in 2026 range
pgvector==0.3.6           # for VECTOR type in SQLAlchemy/SQLModel
psycopg2-binary==2.9.10   # sync driver (simple for start)
alembic==1.14.0           # migrations later
python-dotenv==1.0.1
sentence-transformers==3.3.1  # embeddings
torch==2.5.0        # cpu only for mini-project (or torch if you have GPU)
numpy==2.1.2
pydantic==2.10.0
pulp==2.9.0
numpy==2.1.2  # already have, but ensure